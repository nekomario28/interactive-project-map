{
  "name": "github-project-galaxy-api",
  "version": "0.1.0",
  "private": true,
  "description": "Turn a GitHub user's public repositories into an embeddable galaxy map, JSON graph API, and interactive viewer.",
  "type": "module",
  "engines": {
    "node": ">=22.18.0"
  },
  "scripts": {
    "dev": "wrangler dev",
    "deploy": "wrangler deploy",
    "typecheck": "tsc --noEmit",
    "check:worker": "wrangler deploy --dry-run --outdir .wrangler/dry-run",
    "check:action": "node --check scripts/action.mjs",
    "check:pages": "node --check scripts/github.mjs && node --check scripts/graph.mjs && node --check scripts/svg.mjs && node --check scripts/build.mjs && node --check scripts/pages-app.mjs",
    "test": "node --test tests/*.test.mjs",
    "verify": "npm run typecheck && npm run check:worker && npm run check:action && npm run check:pages && npm test",
    "build:pages": "node scripts/pages-app.mjs",
    "build:pages:catalog": "node scripts/build.mjs"
  },
  "devDependencies": {
    "typescript": "^7.0.2",
    "wrangler": "^4.118.0"
  }
}
